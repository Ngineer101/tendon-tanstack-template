# Better Auth configuration
BETTER_AUTH_URL=http://localhost:3000
BETTER_AUTH_SECRET=

# Stripe test-mode configuration
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRO_MONTHLY_PRICE_ID=
STRIPE_CREDITS_1000_PRICE_ID=
STRIPE_CREDITS_5000_PRICE_ID=
STRIPE_CREDITS_20000_PRICE_ID=

# Optional: enable Stripe Tax after configuring it in the Dashboard.
STRIPE_TAX_ENABLED=false

# MCP server connections
# 32-byte random key, base64url-encoded. Generate with:
#   node -e "console.log(crypto.randomBytes(32).toString('base64url'))"
# Store via `wrangler secret put MCP_ENCRYPTION_KEY`. NEVER commit a real key.
MCP_ENCRYPTION_KEY=
